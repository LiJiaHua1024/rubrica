构件与它守的规矩：

断行
: 整段一起求解，不是填满一行算一行。代价函数把每一行的富余量算进总和，所以一行不会为了自己好看而把下一行挤坏。

悬挂缩进
: 记号挂在页边留出的空位里。
: 正文从第二行起按去掉记号后的栏宽走，所以一格里的长句也要换行。

混排
: 汉字与拉丁字母各由承担它的字体来画，边界上是约四分之一 em 的可调间距。

普通的一段话也会
在作者的换行处折下去，
它不应该被当成定义列表。
