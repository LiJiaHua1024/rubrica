//! Built-in document shown when no file is given.
//!
//! Not a marketing page: every block here exists to make one engine behaviour
//! visible, so that opening the app with no arguments is itself a regression test
//! you can read. Mixed-script runs carry no typed spaces around the Latin words,
//! because that is the case the glue model has to solve on its own.

pub const DOCUMENT: &str = r#"# Rubrica

一份**专注排版的** Markdown 阅读器。正文由 DirectWrite 测量，断行由自己的 Knuth–Plass 求解器决定，整个过程没有 webview。

## 全局断行

贪心算法逐行填满，然后把剩下的参差不给任何补偿。全局断行会同时权衡整段的所有合法断法，选择总代价最小的一组，所以每一行的拉伸量都接近——下面这段没有任何手工调整的空格，行尾依然是齐的。排版的质量通常不在单行上显出来，而是在连续十几行的节奏里；一行难看也许看不出来，十行参差一眼就能看见，而读者的眼睛并不会明确指出它不满的理由，它只是更早疲劳。

## 中西文混排

引擎在 `Han` 与 `Latin` 的边界自动插入约四分之一 em 的可调间距，作者不需要手动加空格：使用Rust编写、Direct2D绘制、以及Microsoft YaHei渲染中文。这些边界上的 glue 既能拉伸也能压缩，因此在两端对齐时它们会和中文ideograph之间的缝隙一起承担富余量。

- 中文ideograph之间使用可压缩 glue，所以没有空格的段落也能对齐
- 西文单词只在 UAX #14 允许的位置断行
- 行首禁则由 Unicode 表提供，不再另立一份容易漂移的清单

## 代码与标题

标题和代码块一律左对齐。把标题拉伸到整个栏宽是排版错误，所以 `BreakOptions::ragged` 会为这类块放弃两端对齐：

```rust
let opts = BreakOptions::new(column);
opts.ragged = true; // headings, code
```

> 引用块用左侧竖线标出层级，嵌套两层仍然可读。
>
> 段间距按正文行高的倍数给出，而不是固定像素，这样字号变化时节奏不会散掉。

## 度量

中文需要比西文更大的行高：ideograph 是方形的，西文靠下伸部之间的空隙分行，中文没有这个空隙。同一字号下，正文行高取 1.7 与 1.95 的差别就在这里。

1. 栏宽上限约 36 em
2. 正文 13.5 磅，约合 96 dpi 下的 18 像素
3. 大字号标题反向收紧字距

## 悬挂缩进

列表的记号（圆点、数字、勾选框）不是正文的第一个词，它挂在页边留出的空位里：从第二行起，每一行都按去掉记号宽度后的栏宽断行，所以记号既不会把正文推走，正文也不会压到它下面。

1. 第一项要写得足够长，长到必须换行，才能看出第二行的起点是不是落在第一项正文的左边沿上
2. 数字用的是等宽数字，所以第 9 项与第 10 项的正文右移的距离相同
3. 第三项里嵌一层列表：
   - 嵌套层的记号再向右让开一级，它自己的换行仍然只按它自己的记号宽度
   - 这一项同样要长到换行，才能看出两层的记号各归各的位置
4. 有序列表的记号取自它自己的编号，而不是按 em 估一个宽度：一个 `10. ` 比 `• ` 宽，二者下方对齐的正文也就不该在同一处
5. 脚注的编号用同一套机制，只是对齐的宽度由整个注释区里最宽的那个编号决定，于是 9 和 10 号的正文仍然落在同一条竖线上
6. 一个条目可以不止一段，而记号只属于开个头的那一段

   这就是它的第二段。没有记号可挂的段落不再让出记号的宽度，它直接从正文栏的左边沿起排，和上面那行的正文落在同一条竖线上。

   ```sh
   $ rubrica-app --report --width 900
   ```

   代码块与引用都跟着同一条规矩：它们是这一项的内容，不是页边的另一个段落。

## 表格

列宽由这一列最宽的一格决定，而不是把栏宽平分：一列只装得下两个数字，它就不该占去半页。格子里的文字永不两端对齐，短标签被拉满一列只会把字撕开；行高按换行后的结果量出，所以一格排到四行，这一行就长到四行，而不是压到下一行上面。

| 构件 | 它守的规矩 | 序 |
|:-----|:-----------|---:|
| 断行 | 整段一起求解，不是填满一行算一行 | 1 |
| 悬挂缩进 | 记号挂在页边留出的空位里，正文从第二行起按去掉记号后的栏宽走，所以一格里的长句也要换行[^tb] | 2 |
| 混排 | 汉字与拉丁字母各由承担它的字体来画<br>中间再加一段窄空 | 3 |
| 删除线 | ~~这一句在一格里被划掉~~：线的高度仍由画这两个脚本的那两张字体各自定 | 4 |

`构件` 那一列左对齐，`序` 那一列右对齐且用等宽数字，于是三个个位落在同一条竖线上；一格里换行只有 `<br>` 这一种办法，它和正文里的换行是同一件事。

## 阅读偏好

字号不是排版的敌人，读者的眼睛才是。窗口没有菜单栏，所以这些都在键盘上：`Ctrl`+`+` 与 `Ctrl`+`-` 放大和缩小，`Ctrl`+`0` 回到设计字号（也可以按住 `Ctrl` 滚轮）。放大是整页等比放大——行宽、缩进、段距与正文同为一个字号的倍数，所以一行容纳的字数不变，变的只是字的大小。缩放不会把你当前位置弄丢：滚动量随字号一起换算，按下键时看到的那一行还在原处。

`Ctrl`+`D` 在白与黑之间切换，并且盖过系统的深浅色设置不放：亮房间里读正文的读者，和暗房间里读正文的读者，对一个键的份量是一样的。

## 指针

强调色是一种承诺：指针移到链接上会变成一只手，按下去就交给系统默认的程序去打开。链接换行之后两行都可点——可点的是纸上的一块地方，不是一个字符串，所以 [这个跨了行宽的目标在两行上各占一块位置，指针落在哪一行都算落在它上面](https://spec.commonmark.org/0.12/)。

脚注的数字[^dy]也是一块可点的地方，但它不打开别处：注释本来就在同一张纸的下方，按下只是把页面带到它跟前，顶上仍然留一行正文，让读者知道自己是打哪儿来的。

不打算跟从的地址连颜色也不给。[这个相对链接](other.md) 指向这台机器上的别处，按下去不会得到任何东西，所以它看起来就是一句正文——把它写成普通文字，比涂成一种兑现不了的承诺诚实；`file:` 与绝对路径同理，一篇被阅读的文章不该借一次点击去启动什么。

## 删除线

~~这一句被划掉了~~：线的位置取自字体自己的 OS/2 表（yStrikeoutPosition 与 yStrikeoutSize），而不是按 em 猜一个高度。混排的 ~~中文mixed~~ 由承担这两个脚本的两个面各自画出自己的一段，因为它们的 x 高度本来就不同；换行之后每一段各得一条线，而不是一条长线跨过页宽。

## 任务清单

勾选框由符号字体承担：正文里没有 U+2610 这个字形时，逐字的回退链会把它交给符号字体，而不是留下一个空心方块。

- [ ] 未完成的项目显示一个空盒
- [x] 已完成的项目显示一个打勾的盒

## 数学排版

行内公式 $\int_0^1 x^2 dx = \frac{1}{3}$ 与相邻的中文共用一条基线：它的盒子带着自己的升部与降部，所以这一行会被撑高，而不是把公式压进正文已有的行距里。

$$\sum_{n=1}^{\infty} \frac{1}{n^2} = \frac{\pi^2}{6}$$

$$f(x) = \begin{cases} 1 & x \ge 0 \\ -1 & x < 0 \end{cases}$$

$$\begin{pmatrix} a & b \\ c & d \end{pmatrix} \begin{pmatrix} x \\ y \end{pmatrix} = \begin{pmatrix} ax + by \\ cx + dy \end{pmatrix}$$

上面每一个数字都取自字体自带的 MATH 表：分数线落在 axisHeight 上，分子按 numShift 上移，根号与括号由 glyph assembly 拼出需要的高度，而不是把一个字形纵向拉长。多行的公式也一样——行距取 mathLeading，整块以 axisHeight 为轴居中，所以矩阵和它旁边的分数线同高。

## 脚注

引用标记只把数字抬高，让它坐在它所属的那个词上方，而不是成为句子里的一个词：全局断行的代价函数[^dy]决定了每一行的富余量，重复引用同一个脚注得到的是同一个数字[^dy]。

[^dy]: 上标和公式的下标是同一套机制：渲染的每一段文字本来就带着自己的 drop，取负就是向上，所以行高会把抬起来的部分算进升部，不会被上面一行切掉。

[^margin]: 这一条没有任何引用指向它，仍然排在文末：作者写下的文字不该因为少了一个标记就消失。

[^tb]: 脚注编号也能落在一格里：它先是这一格文字的一部分，跟着这一格换行，然后才是整页上的一个跳转目标。
"#;
